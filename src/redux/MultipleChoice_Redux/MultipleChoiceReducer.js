import MultipleChoice from "../../components/MultipleChoice"

const initialState = {
    multiple_final_answer_array = []
}

const MultipleChoiceReducer = (state = initialState, action) => {
    switch(action.type){
        case MultipleChoice:

        default: return state 
    }
}

export default MultipleChoiceReducer